import 'dart:ui';

  abstract class Constants {
  static const Color kPrimaryColor = Color(0xff100B20);
  static const ketTransitionDuration = Duration(milliseconds: 250);
  static const kFeaturedBox = 'featured_box';
}
